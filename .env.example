# 网易云音乐 API 代理地址
# 部署方式：git clone https://github.com/Binaryify/NeteaseCloudMusicApi && npm install && node app.js
# 然后将下面的 URL 改为你实际部署的地址（同域或子域均可）
PUBLIC_NETEASE_API=https://your-netease-api.example.com
